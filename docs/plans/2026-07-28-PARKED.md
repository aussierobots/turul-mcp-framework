# Branch parking record — `feat/turul-mcp-protocol-2026-07-28`

**Last updated**: 2026-06-01 (Slice A' + Slice A'' + Slice B blocker remediation + codex P1-P3 + Slice C (spec-coexistence doc package) + Slice D (codex first-pass P0-P2 remediation) + Slice E (codex second-pass: ADR-030 cargo design re-correction + Phase 9.4 drift mop-up) + Slice F (codex third-pass: rollout plan Phase 4 + ADR-030 narrowing shorthand cleanup) + **Slice G (codex fourth-pass P3: revision-log hygiene — SUPERSEDED/OPERATIVE labels on historical revision-log entries that previously quoted the broken intermediate Cargo design)** landed; tree is clean to commit)
**Branch**: `feat/turul-mcp-protocol-2026-07-28` (sub-branch of `2026-07-28-MCP-Specification`)
**Tip (committed)**: `0937d1c refactor(protocol-2026-07-28): tighten schema fidelity, fix doc/trait drift`
**Working tree**: **100 tracked-modified + 7 untracked = 107 status entries**. All uncommitted; two/three commits' worth of work staged, awaiting maintainer commit authorization.
**Not pushed**: no upstream tracking; all work is local.
**`main`**: untouched; 5 commits ahead on this branch.

This document is the parking-state record. Anyone resuming this branch should read this first, then `git status` + `git log main..HEAD`.

## Branch-lock reminder

- **DO NOT merge `2026-07-28-MCP-Specification` into `main` without express maintainer authorization.** No fast-forward, no rebase-onto-main, no squash, no merge PR.
- **DO NOT delete the branch, force-push it, or treat it as "complete"** without express authorization. Tests pass + compliance green is necessary but not sufficient — final disposition is the maintainer's call.
- `main` stays on 2025-11-25 until the maintainer chooses to cut over.

## Stop condition

- **Slice A (initial schema fidelity)** — committed (5 commits ending at `0937d1c`).
- **Slice A' (eight follow-up schema-fidelity + trait-surface corrections)** — landed in working tree; 338 tests pass, 0 warnings.
- **Slice B (governance docs + Cargo manifest migration + ADR + CHANGELOG)** — landed in working tree; all four PARKED.md-identified blockers resolved.
- Tree is clean to commit. No outstanding blockers known.

## Slice A — committed history (5 commits ahead of main)

```
0937d1c refactor(protocol-2026-07-28): tighten schema fidelity, fix doc/trait drift
c0737fb fix(workspace): isolate turul-rpc 0.2.1 to protocol-2026-07-28 crate
daaa5b9 refactor(protocol-2026-07-28): require _meta on RequestParams extenders, remove non-spec fields
064733e refactor(protocol-2026-07-28): spec-align wire layer, migrate to turul-rpc 0.2.1
16fbb86 docs(protocol-2026-07-28): add src/ with spec-anchored comments
```

## Slice A' — schema-fidelity corrections (uncommitted)

Surfaced by an internal multi-agent review (workflow `wf_d6984699-a5a`, 2026-05-31) and adversarially verified. **Eight defects fixed; +16 regression tests.**

| ID | Defect | Fix | Schema anchor |
|---|---|---|---|
| **A1** | `meta::ProgressToken(pub String)` rejected numeric tokens; schema declares `string \| number`. `notifications::ProgressTokenValue` had the correct enum on the notification side — two non-interoperable bindings of one schema type. **codex P1 follow-up (2026-05-31)**: initial fix used `Number(i64)` which still rejected JSON floats like `1.5` — a `number` in the schema is any IEEE-754 double. | Final form: untagged enum `ProgressToken { String(String), Number(serde_json::Number) }`. `serde_json::Number` losslessly preserves int vs float and accepts negative / large integers / floats. `From<i64>` / `From<u64>` / `From<i32>` / `From<f64>` impls. Both carriers (`RequestMetaObject.progressToken?` and `ProgressNotificationParams.progressToken`) use the unified type; `ProgressTokenValue` aliases it. | `ProgressToken` type alias |
| **A2** | `SubscriptionsListenRequestParams._meta: Option<HashMap>` while schema says `extends RequestParams` ⇒ required typed `RequestMetaObject`. The lone outlier among `RequestParams` extenders. Falsified a ✅ row in published COMPLIANCE.md. | Changed to required `meta: RequestMetaObject`; constructors take meta. | `SubscriptionsListenRequestParams extends RequestParams`; `RequestParams._meta: RequestMetaObject` |
| **A3** | `CancelledNotificationParams.request_id` was required; schema declares it optional (`requestId?`). Spec text explicitly permits late-arriving cancellations after the request finishes. | Changed to `Option<RequestId>` with `skip_serializing_if`; added `CancelledNotification::without_id()` constructor. | `CancelledNotificationParams.requestId?: RequestId` |
| **A4** | `ContentBlock::ResourceLink` embedded a `ResourceReference` missing `size` and `icons` — but schema says `ResourceLink extends Resource` which has both via `extends Icons`. Silent wire drop. | Added `size: Option<u64>` + `icons: Option<Vec<Icon>>` (with `with_size`/`with_icons` builders) to `ResourceReference`. | `ResourceLink extends Resource`; `Resource extends BaseMetadata, Icons` |
| **A5** | `ElicitationSchema` was missing the `$schema?: string` field central to DRAFT-2026's JSON Schema 2020-12 dialect adoption. Wire `{"$schema": "..."}` round-tripped to silent drop. | Added `schema_dialect: Option<String>` with `#[serde(rename = "$schema")]` + `with_schema_dialect` builder. | `ElicitRequestFormParams.requestedSchema.$schema?: string` |
| **A6** | `ListRootsRequest.params` used bespoke `Option<ListRootsParams { meta: Option<HashMap> }>` instead of schema's `params?: RequestParams`. Inconsistent with sibling `RequestParams` extenders. | Replaced with `params: Option<crate::json_rpc::RequestParams>`. `ListRootsParams` struct removed. | `ListRootsRequest.params?: RequestParams` |
| **A7** | Seven notification traits in `traits.rs` (`*ListChangedNotification`, `ProgressNotification`, etc.) were bound on `JsonRpcNotificationTrait` requiring `HasJsonRpcVersion` — a field the inner notification structs intentionally don't carry (envelope added by wrapping in `JsonRpcNotification` at transport time). Traits were unimplementable. | Rebound on `RpcNotification` (`HasMethod + HasParams` only); renamed with `*Trait` suffix to avoid struct-name collisions; concrete impls added. Trait abstraction now matches the Rust split (inner-payload-only), documented intentional deviation from schema-interface wire-completeness. | n/a (Rust trait design — schema's `*Notification extends JSONRPCNotification` interfaces ARE wire-complete; Rust split is documented in §Intentional deviations) |
| **A8** | Missing trait coverage for new DRAFT-2026 RPCs — `server/discover`, `subscriptions/listen`, `InputRequiredResult` (SEP-2322). | Added `DiscoverRequestTrait`, `SubscriptionsListenRequestTrait` + `HasSubscriptionsListenParams`, `HasInputRequiredResult`. Impls in `discover.rs`, `subscriptions.rs`, `input_required.rs`. | `DiscoverRequest`, `SubscriptionsListenRequest`, `InputRequiredResult` |

### Verification state at end of Slice A' (post-codex P1 fix)

- `cargo check -p turul-mcp-protocol-2026-07-28 --features compliance` — 0 warnings.
- `cargo test -p turul-mcp-protocol-2026-07-28 --features compliance` — **342 passing** (159 lib + 179 integration + 3 fixture + 1 doctest), 0 failed. (Down by 1 from Slice A' tip: `logging::tests::test_logging_message_notification` was removed alongside the duplicate struct it exercised; identical coverage already exists at `notifications::tests::test_logging_message_notification`.)
- `cargo doc -p turul-mcp-protocol-2026-07-28 --no-deps` — 0 warnings (default features).
- Compliance harness pinned to upstream commit `c3e3f09e...`.
- COMPLIANCE.md updated to reflect Slice A' corrections (new §"Schema-fidelity corrections (Slice A' follow-up, 2026-05-31)" + slimmed §"Known follow-ups"). ProgressToken row reflects the `serde_json::Number` form.
- ADR-027 revision log carries the full Slice A' entry.

## Slice A'' — SEP-2577 deprecation annotations (uncommitted)

Surfaced by the 14-axis deprecation+changelog audit workflow (2026-05-31). Landed because SEP-2577 explicitly mandates annotation-only deprecation marking in this revision, and parking without the annotations leaves a spec-conformance gap on a documented requirement.

| Feature | Action | Files touched |
|---|---|---|
| **Roots** | `#[deprecated]` on `Root`, `ListRootsRequest`, `ListRootsResult`, `RootsCapabilities`, `traits::ListRootsResult`. `#[allow(deprecated)]` on impl blocks, the `InputRequest::ListRoots` variant, and the test module. Module-level header explains migration path. | `src/roots.rs`, `src/initialize.rs`, `src/input_required.rs`, `src/traits.rs`, `src/prelude.rs`, `src/compliance/coverage.rs` |
| **Sampling** | `#[deprecated]` on `SamplingMessage`, `SamplingMessageContent`, `SamplingMessageContentBlock`, `CreateMessageRequest`, `CreateMessageRequestParams`, `CreateMessageResult`, `SamplingCapabilities`, `traits::HasCreateMessageRequestParams`, `traits::CreateMessageRequest`, `traits::CreateMessageResult`. `#[allow(deprecated)]` cascade on every trait impl + the test module. `Role`, `ModelPreferences`, `ToolChoice`, `ToolChoiceMode`, `ModelHint` left un-deprecated (used outside sampling or by the SEP-2322 MRTR flow). | `src/sampling.rs`, `src/initialize.rs`, `src/input_required.rs`, `src/traits.rs`, `src/param_extraction.rs`, `src/prelude.rs` |
| **Logging** | `#[deprecated]` on `LoggingMessageNotification`, `LoggingMessageNotificationParams`, `traits::LoggingMessageNotificationTrait`. `LoggingLevel` / `LogLevel` left un-deprecated (still the value type for the non-deprecated `RequestMetaObject.log_level` replacement). | `src/logging.rs`, `src/notifications.rs`, `src/traits.rs`, `src/lib.rs`, `src/prelude.rs` |
| **Pre-existing duplicate fixed** | `LoggingMessageNotification` was defined twice (`logging.rs` with `LoggingMessageParams`, `notifications.rs` with `LoggingMessageNotificationParams`). Removed the `logging.rs` duplicate (struct + impls + tests); moved `HasLevelParam` / `HasLoggerParam` / `HasMetaParam` impls onto the spec-aligned `notifications::LoggingMessageNotificationParams`. | `src/logging.rs`, `src/notifications.rs` |

**Verification at end of Slice A''**:
- `cargo check --features compliance` — 0 warnings.
- `cargo test --features compliance` — 342 passing (159 lib + 179 + 3 + 1).
- `cargo doc --no-deps` — 0 warnings.

**Annotation strategy decision**: per-type inline `#[deprecated(since = "0.4.0", note = "...")]` with full migration-path message (rejected alternatives: bare `#[deprecated]`, module-only `//!` docs without attributes, central re-export module). Reasoning recorded in the audit synthesis report (workflow `wf_1288b264-b53` §6).

## Slice B — governance + Cargo manifest migration (uncommitted)

### Working-tree inventory (90+ files, governance + Cargo manifest only)

| Group | Count | Files |
|---|---|---|
| Governance docs | 2 | `AGENTS.md`, `CLAUDE.md` |
| Existing ADR updates | 1 | `docs/adr/027-targeting-mcp-draft-2026-v1.md` (revision log + carve-out language) |
| New ADRs | 1 | `docs/adr/028-extensions-strategy.md` |
| New plan docs | 4 | `docs/plans/2026-07-28-{compliance-plan,example-fixture-compliance,migration-diff,schema-coverage-matrix}.md` |
| This parking record | 1 | `docs/plans/2026-07-28-PARKED.md` |
| Frozen-crate manifests | 2 | `crates/turul-mcp-protocol-2025-{06-18,11-25}/Cargo.toml` (literal `0.3.47` pin) |
| Non-frozen workspace crate manifests | 12 | All other `crates/*/Cargo.toml` (literal `0.4.0`) |
| Examples Cargo.toml | 61 | `examples/*/Cargo.toml` (literal `0.4.0`) |
| Tests Cargo.toml | 8 | `tests/*/Cargo.toml` (literal `0.4.0`) |
| CHANGELOG | 1 | `CHANGELOG.md` (new `[0.4.0]` entry) |
| Protocol crate sources | many | Slice A' edits (under `crates/turul-mcp-protocol-2026-07-28/`) |

All Cargo.toml changes are the same mechanical pattern: `version.workspace = true` → literal `version = "X.Y.Z"`.

### PARKED.md blocker resolution

| Blocker | Resolution |
|---|---|
| **B1** AGENTS.md still had 2025-11-25 stale guidance unqualified | New §"Branch-Conditional Spec Guidance" header added; all 9 confirmed hits section-marked with `(2025-11-25 baseline)` qualifiers or moved into the Branch Lock section. Verify: `grep -nE '\b2025-11-25\b' AGENTS.md` — every hit is either factual/structural, section-marked, or in the Branch Lock context. |
| **B2** Frozen-crate Cargo.toml + root Cargo.toml referenced CLAUDE.md/AGENTS.md by name | All 4 self-references replaced with self-contained explanatory comments. Verify: `grep -rnE 'CLAUDE\.md\|AGENTS\.md' Cargo.toml crates/*/Cargo.toml` returns empty. |
| **B3** ADR-027 needed revision-log entry for per-crate versioning + frozen-manifest touch | Two new revision-log entries dated 2026-05-31: per-crate versioning adoption (invoking the CLAUDE.md carve-out for the one-time frozen-manifest touch); Slice A' schema-fidelity corrections. §Consequences — the frozen-crate carve-out language and the workspace-version status bullet both updated. |
| **B4** CHANGELOG.md missing `[0.4.0]` entry though workspace already at 0.4.0 | New `[0.4.0] - Unreleased (feature branch ...)` section added with release-status banner. Lists the new 2026-07-28 protocol crate, per-crate independent versioning policy, frozen-crate carve-out, turul-mcp-json-rpc-server shim status, and downstream-consumer notes. Footer compare-link deferred to release tag. |

### Recommended commit sequence (when authorized)

1. **Slice A'** — single commit covering `crates/turul-mcp-protocol-2026-07-28/src/**` + `tests/**` + `COMPLIANCE.md` (Slice A' rows only). Subject: `fix(protocol-2026-07-28): schema-fidelity + trait-surface corrections (A1-A8)`. Body summarizes A1-A8 per the Slice A' table above.
2. **Slice A''** — single commit landing the SEP-2577 deprecation annotations. Files: `src/roots.rs`, `src/sampling.rs`, `src/logging.rs`, `src/initialize.rs`, `src/notifications.rs`, `src/traits.rs`, `src/input_required.rs`, `src/param_extraction.rs`, `src/prelude.rs`, `src/lib.rs`, `src/compliance/coverage.rs`, and the Slice A'' section of `COMPLIANCE.md`. Subject: `feat(protocol-2026-07-28): annotate Roots/Sampling/Logging as #[deprecated] per SEP-2577`. Body lists the annotated types and notes the duplicate `LoggingMessageNotification` reconciliation.
3. **Slice B-1 governance** — `CLAUDE.md`, `AGENTS.md`, `docs/adr/027-...`, `docs/adr/028-...`, `docs/plans/2026-07-28-*.md` (existing), `CHANGELOG.md`. Subject: `docs: dual-branch governance + 0.4.0 changelog entry`.
4. **Slice B-2 mechanical** — all 84 member `Cargo.toml` files. Subject: `chore(workspace): adopt per-crate independent versioning; freeze 2025-* at 0.3.47`. Single atomic commit so bisect treats it as one unit.
5. **Slice C (spec-coexistence doc package)** — the four new docs and eight amendments landed on 2026-05-31 documenting the locked default-2026 + bilingual-client decision. Files:
   - `docs/adr/029-spec-coexistence-via-cargo-features.md` (NEW; the load-bearing ADR; default = DRAFT-2026-v1 via mutually-exclusive cargo features at `turul-mcp-protocol`)
   - `docs/adr/030-turul-mcp-client-bilingual-spec-coexistence.md` (NEW; per-connection version detection; bilingual default; `client-2025-only`/`client-2026-only` narrowing features)
   - `docs/plans/2026-07-28-architecture-review.md` (NEW; doc-form persistence of the 5-pattern review workflow)
   - `docs/plans/2026-07-28-feature-gating-rollout.md` (NEW; per-crate `#[cfg(feature = "...")]` rollout plan)
   - `docs/adr/027-targeting-mcp-draft-2026-v1.md` (AMENDED; §Consequences replaced + §Status update inserted + revision-log entry — supersedes the "deferred Phase 9.4" framing)
   - `docs/adr/006-streamable-http-compatibility.md` (AMENDED; new §"DRAFT-2026-v1: Stateless variant; GET SSE is 2025-only")
   - `docs/adr/009-protocol-based-handler-routing.md` (AMENDED; `McpProtocolVersion` becomes feature-exclusive)
   - `docs/adr/023-tool-change-detection-and-notification.md` (AMENDED; per-request fingerprint persistence in stateless mode)
   - `docs/adr/001-lambda-mcp-integration-architecture.md` (AMENDED; stateless 2026 Lambda variant — ~50 LOC vs ~200 LOC stateful)
   - `docs/adr/025-extract-turul-rpc.md` (AMENDED revision log; turul-rpc cutover stays scheduled for 0.4.0 atomically with Phase 9.4)
   - `docs/adr/026-lambda-streaming-empty-body-contract.md` (AMENDED revision log; empty-body envelope is moot in 2026 mode because GET SSE is not served)
   - `docs/adr/028-extensions-strategy.md` (AMENDED revision log; ext-tasks/ext-apps remain post-0.4.0)
   - this PARKED.md (cross-reference)
   
   Subject: `docs(adr): spec-coexistence decision package (ADR-029 + ADR-030 + 8 amendments)`.
   Body summarizes: default = 2026 via cargo features; bilingual client; Phase 9.4 is in-scope for 0.4.0 (not deferred); 0.4.0 NOT shippable until consumer migration + workspace turul-rpc bump + legacy E2E coverage land.
6. **Slice D (codex first-pass P0/P1/P2 remediation)** — landed 2026-05-31 after Slice C reviewed by codex. Roll into Slice C as a fixup, OR keep as its own commit. Files:
   - **Code (test-attribute additions)**: `crates/turul-mcp-protocol-2026-07-28/src/notifications.rs`, `src/input_required.rs`, `src/elicitation.rs` (`#[allow(deprecated)]` on `mod tests`), and `tests/compliance.rs` (`#![allow(deprecated)]` at crate root). These are arguably part of Slice A'' (they make Slice A''s `#[deprecated]` annotations test-clean). Recommend: squash into Slice A''.
   - **Different crate**: `crates/turul-mcp-client/tests/wire_compliance.rs` — one comment rewritten to drop a CLAUDE.md citation per the §Comments rule (P2-6). Standalone fixup or fold into Slice C.
   - **Doc edits**: ADR-029 §"Cascade rule" rewritten + revision-log correction entry; ADR-030 cargo topology + fallback tightened + 3 revision-log entries; ADR-027 Phase 9.4 reconciliation; PARKED.md + codex-summary Phase 9.4 wording; COMPLIANCE.md L77 contradiction fixed; feature-gating-rollout.md Phase 0/4 alignment; CHANGELOG.md "no consumer migration" claim corrected. Recommend: roll into Slice C.

   Subject (if standalone): `docs(adr): codex remediation passes 1-4 (P0 cargo-topology + P1 Phase 9.4 drift + P2 cleanup)`.

7. **Slices E + F + G** — codex passes 2/3/4 each refined the doc set further. None added new files; all touched ADR-029, ADR-030, PARKED.md, codex-summary.md, CHANGELOG.md, and `feature-gating-rollout.md`. Recommend: fold into the same commit as Slice D, since they refine the same doc set. The fourth-pass P3 revision-log hygiene fix is the smallest of these.

All seven slices are bisectable individually if kept separate. The pragmatic grouping for the next session: **(A'), (A'' + D's test-attribute additions), (B-1), (B-2), (C + D's doc edits + E + F + G), (D's wire_compliance.rs as a standalone client-test fixup)** — six commits total, or fewer if the maintainer prefers squashing.

## What's specifically NOT done (carry forward)

- **`ContentBlock` union split** — schema declares two unions (`ContentBlock` with `ResourceLink`/`EmbeddedResource`, `SamplingMessageContentBlock` with `ToolUse`/`ToolResult`); Rust uses a single 7-variant enum. Wire-equivalent for the 3-variant overlap. Documented in §Intentional deviations. Will be re-evaluated at the next major spec-fidelity slice.
- **`ResourceReference` ↔ `Resource` duplication** — A4 closed the wire-shape gap (size/icons now present on `ResourceReference`), but the two structs remain parallel. Collapsing onto one canonical `Resource` (per `ResourceLink extends Resource`) is the cleaner end-state. Queued for follow-up.
- **Coverage expansion** — 78 of 86 upstream example dirs still `Kind::NotModeled` in `compliance/coverage.rs`. Floor stays at 8.
- **Downstream consumer migration (cut-over)** — `turul-mcp-builders`, `turul-mcp-server`, `turul-http-mcp-server`, `turul-mcp-client`, `turul-mcp-aws-lambda`, derive macros, 55 example crates have NOT been updated for the breaking API changes that landed in Slice A + Slice A'. They still depend on `turul-mcp-protocol` (the 2025-11-25 re-export). ADR-027 Phase 9.4 ("flip the alias") is the cutover slice; **strategy is committed per ADR-029 §"What the cutover slice ships" item 5: flip-all-at-once** (atomicity enforced by the feature-gated re-export's `compile_error!` guards). The earlier three-way question (flip-all-at-once vs dual-import vs crate-by-crate) is closed.
- **turul-rpc workspace bump** — the workspace pin is `0.1`; the 2026-07-28 protocol crate isolates `0.2.2` per ADR-025. A workspace-wide `0.1 → 0.2.x` bump is a prerequisite for consumer migration, independently mergeable to `main`. **Not yet scoped.**
- **Push** — branch has no upstream tracking; nothing pushed anywhere.

## Resume here

When resuming:

1. Read this file in full.
2. `git status --short | wc -l` — expect ~125 dirty entries (113 modified + 12 untracked at park time).
3. `git log main..HEAD --oneline` — expect 5 already-committed commits ahead of `main` (tip `0937d1c`). Uncommitted work in the dirty tree is the seven slices documented above.
4. `cargo test -p turul-mcp-protocol-2026-07-28 --features compliance` — expect **342 passing** (159 lib + 179 integration + 3 fixture + 1 doctest), **0 deprecation warnings**, 0 failed.
5. `cargo doc -p turul-mcp-protocol-2026-07-28 --no-deps` — expect 0 warnings.
6. Decide commit authorization. Sequence: Slice A' → Slice A''+D-test-attrs → Slice B-1 → Slice B-2 → Slice C+D-doc-edits+E+F+G → optional Slice D-wire_compliance-fixup (six commits total per the suggested grouping in §"Recommended commit sequence"). If the maintainer prefers, all seven slices can be kept as separate bisectable commits.
7. **Do not push, merge, or open a PR** without explicit maintainer authorization per the branch lock.
8. If continuing technical work after the commit sequence lands: execute Phase 9.4 per ADR-029's flip-all-at-once commitment (no further strategy ADR needed — that decision is recorded in ADR-029 §"What the cutover slice ships" item 5). The next prerequisite is the workspace `turul-rpc` 0.1 → 0.2.2 bulk pin (also called out in ADR-029).
9. Codex review state at park time: **fourth-pass verdict "No P0/P1/P2 issues found"** (P3 verification-claim hygiene addressed in Slice G). Branch is technically clean for any pre-implementation review; the next codex pass — if invited — should focus on the *implementation* slice once Phase 9.4 begins, not the decision docs.

The branch lock in CLAUDE.md remains binding: **no merge to `main` without express maintainer authority**, regardless of how green the tests are.
