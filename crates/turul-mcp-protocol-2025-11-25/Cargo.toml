[package]
name = "turul-mcp-protocol-2025-11-25"
# Frozen historical spec snapshot — last 0.3.x publish, never bumped or edited
# again. Literal `version` pinned because the workspace adopted per-crate
# independent versioning at 0.4.0 and this crate does NOT move to that line.
version = "0.3.47"
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Model Context Protocol (MCP) specification implementation - 2025-11-25 version"

[features]
default = []
server = []  # Server-side features
client = []  # Client-side features

[dependencies]
turul-mcp-json-rpc-server.workspace = true
serde.workspace = true
serde_json.workspace = true
async-trait.workspace = true
thiserror.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }
