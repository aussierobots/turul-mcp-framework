# Vendored SEP-2663 (Tasks extension) schema — provenance

| File | Upstream source |
|---|---|
| `draft-schema.ts` | `modelcontextprotocol/ext-tasks` → `schema/draft/schema.ts` |
| `draft-schema.json` | `modelcontextprotocol/ext-tasks` → `schema/draft/schema.json` |

- **Repository**: https://github.com/modelcontextprotocol/ext-tasks
- **Commit**: `8966bea9c4f4e6d71060cc8284a539086e9e234f` (2026-06-09)
- **Extension identifier**: `io.modelcontextprotocol/tasks`
- **SEP**: https://modelcontextprotocol.io/seps/2663-tasks-extension
- **Status upstream**: Experimental / draft

Re-pin by fetching the same paths at a newer commit and updating this table.
Compliance tests in `src/compliance_test.rs` assert wire shapes against these
files' type definitions — re-run them after any re-pin.
