# Spec-NEUTRAL extension crate (no date suffix), feature-gated per spec lane —
# same pattern as turul-mcp-ext-tasks. Independent semver (SEP-2133 §Evolution).
[package]
name = "turul-mcp-ext-apps"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "MCP Apps extension (io.modelcontextprotocol/ui, SEP-1865) MCP-side types — spec-neutral crate, 2026-07-28 lane first"

[features]
default = ["protocol-2026-07-28"]
protocol-2026-07-28 = ["dep:turul-mcp-protocol-2026-07-28"]

[dependencies]
turul-mcp-protocol-2026-07-28 = { version = "0.4.0", path = "../turul-mcp-protocol-2026-07-28", optional = true }
serde.workspace = true
serde_json.workspace = true
