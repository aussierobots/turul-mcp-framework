# 2025-11-25-ONLY by design: this client is wire-pinned — every request body
# hardcodes protocolVersion 2025-11-25 and exercises the stateful session
# contract (initialize handshake, Mcp-Session-Id, DELETE termination) that the
# 2026 stateless core removed. It is the session regression client for the
# protocol-2025-11-25 opt-in lane.
[package]
name = "session-management-compliance-test"
version = "0.4.0"
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
reqwest.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
[[bin]]
name = "session-management-compliance-test"
path = "src/main.rs"
test = false
